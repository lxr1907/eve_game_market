# 2026-05-15 工作日志

## LP蓝图列表增加更新时间列
- 在 `LpBlueprintView.vue` 左侧蓝图列表每个条目底部增加了"更新于 xx分钟前"的时间显示
- 复用后端已有的 `profit_updated_at` 字段（列表接口已返回）
- 新增 `formatUpdatedAt` 函数：1分钟内显示"刚刚"，1小时内显示"xx分钟前"，24小时内显示"xx小时前"，超过则显示日期+时分
- 样式用 `.bp-updated-at` 灰色小字（10px）展示，不干扰主信息
