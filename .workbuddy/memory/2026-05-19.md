# 2026-05-19

## 拉取最新代码（09:09）

- `git pull` Fast-forward 合并，无冲突
- 从 40cb449 更新到 23d712c
- 更新 22 个文件，新增 4 个文件，950 行新增，152 行删除
- 主要变更：LoyaltyController.js LP 收益逻辑优化、LoyaltyOfferView.vue 大量新增、新增 BlueprintProduct 模型/蓝图导入脚本/索引工具/多物品利润调度器

## API 500 修复 — 订单查询（14:13）

### 问题1：同步触发条件 bug
- OrderController.js getOrders 第 136 行 `if (buyTotal === 0 && sellTotal === 0)` 使用 AND 逻辑
- 买单有数据时（63条），卖单永远不会触发同步
- **修复**：改为分别检查 `needSyncBuy` / `needSyncSell`，各自独立同步

### 问题2：数据库字段溢出
- 欧服 Tritanium 的 `volume_total` 超过 MySQL INT 最大值（21亿）
- 报错：`Out of range value for column 'volume_total' at row 14`
- **修复**：`volume_total`、`volume_remaining`、`minimum_volume` 改为 BIGINT

## LP 利润计算 — infinity/tranquility 无数据原因（14:12）

### 分析
- LP offers 已按数据源从不同 URI 同步（serenity 395, infinity 393, tranquility 395）
- infinity 区域 10000016（长征）市场数据稀疏，仅 110 个 type 有订单
- tranquility type_id 与国服不匹配，API 返回 404
- 利润门槛 1300 过高，所有 offer 被跳过

### 修复：取消非晨曦服务器的刷选限制
- `LoyaltyController.js calculateProfitInternal`
- 添加 `isNonSerenity = datasource !== 'serenity'`
- 无买单：非晨曦服务器直接写入 sell_price=0
- 利润门槛：非晨曦服务器不过滤，所有 offer 都写入

## 收益查询性能优化 — orders 表加索引（14:34）

- getProfitData 的 LEFT JOIN + MAX(price) 子查询导致 orders 全表扫描
- 晨曦 4.5s，欧服 8.4s（仅 1 条数据）
- 添加复合索引 `idx_orders_lookup`：(type_id, region_id, datasource, is_buy_order, price)
- 性能提升：晨曦 0.015s (300x)，欧服 0.011s (763x)
- Order.createTable 增加 addIndexes 方法做防御性索引创建

